# shared Folder

## Purpose
Shared assets and declarations used by runtime and tooling (including local recommended model defaults).

## Contents
- `recommended-model-set.local.json` : Local generated recommended model defaults from provider probe (git-ignored).
- `types.js` : JavaScript source code.

## Artifacts
- `recommended-model-set.local.json` : JSON metadata/config artifact

## Maintenance
- Update this README when files are added, removed, renamed, or when the folder purpose changes significantly.
