# icons Folder

## Purpose
Extension icons and icon source assets (PNG outputs and source prompt metadata/art).

## Contents
- `icon-master-gemini.json` : Prompt/metadata for generated icon source asset.
- `icon-master-gemini.png` : PNG image asset (icon/export/source image).
- `icon128.png` : PNG image asset (icon/export/source image).
- `icon128.svg` : SVG vector image asset.
- `icon16.png` : PNG image asset (icon/export/source image).
- `icon16.svg` : SVG vector image asset.
- `icon256.png` : PNG image asset (icon/export/source image).
- `icon32.png` : PNG image asset (icon/export/source image).
- `icon48.png` : PNG image asset (icon/export/source image).
- `icon48.svg` : SVG vector image asset.

## Artifacts
- `icon-master-gemini.json` : JSON metadata/config artifact
- `icon-master-gemini.png` : image asset/output
- `icon128.png` : image asset/output
- `icon16.png` : image asset/output
- `icon256.png` : image asset/output
- `icon32.png` : image asset/output
- `icon48.png` : image asset/output

## Maintenance
- Update this README when files are added, removed, renamed, or when the folder purpose changes significantly.
