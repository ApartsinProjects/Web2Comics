# Bot Deployment (Render + Cloudflare)

Primary deployment/runbook document:

- [deployment-runbook.md](deployment-runbook.md)

Quick command:

```bash
npm run bot:deploy:auto -- --target render --branch engine --env-only
```
